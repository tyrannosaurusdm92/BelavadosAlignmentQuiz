# TableGate Campaign Studio v7 — Four-Part Extraction

Download all four v7 ZIP archives and extract them into the same parent directory. Allow the common `TableGate_Campaign_Studio_v7` directory to merge. Do not concatenate the ZIP files.

After extraction, host the merged directory on a static HTTPS server and open `tablegate.html`.
