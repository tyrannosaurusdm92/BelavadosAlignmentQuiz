
(function(){
  'use strict';
  const LS = window.LifeSim;
  const e = LS.escape;
  const includesQ = (obj,q) => !q || JSON.stringify(obj).toLowerCase().includes(q);
  const chips = (arr, cls='') => (arr||[]).map(x=>`<span class="tag ${cls}">${e(x)}</span>`).join('');
  LS.activeTab = () => document.querySelector('.tab.active')?.dataset.tab || 'dashboard';
  LS.render = () => { LS.renderRaceCache?.(); const q=(LS.$('globalFilter')?.value||'').toLowerCase(); const s=LS.state; LS.$('resultCount').textContent=`${s.locations?.length||0} locations, ${s.npcs?.length||0} NPCs, ${s.relationships?.length||0} relationship records, ${s.households?.length||0} households, ${s.factions?.length||0} factions.`; LS.renderDashboard(q); LS.renderImports(q); LS.renderLocations(q); LS.renderNPCs(q); LS.renderRelationships(q); LS.renderHouseholds(q); LS.renderFactions(q); LS.renderServices(q); LS.renderSchedules(q); LS.renderEditor(); LS.renderExport(); LS.renderOnyx(); LS.renderTesting(); const json=LS.$('json'); if(json) json.textContent=JSON.stringify(s,null,2); };
  LS.renderDashboard = () => { const s=LS.state, c=s.config||{}; const warnings=s.validations||[]; const axes=LS.data.alignment.axes||[]; const raceCounts={}; (s.npcs||[]).forEach(n=>raceCounts[n.race]=(raceCounts[n.race]||0)+1); const topRaces=Object.entries(raceCounts).sort((a,b)=>b[1]-a[1]).slice(0,8).map(([k,v])=>`${k}: ${v}`); const worldTravel=(s.npcs||[]).filter(n=>n.worldTraveler).length; const tz=c.provinceTime||LS.provinceMeta?.(c.province)||{}; LS.$('dashboard').innerHTML=`<div class="statline"><div class="stat"><b>${e(c.settlementName||'No settlement')}</b><br>Settlement</div><div class="stat"><b>${(s.locations?.length||0).toLocaleString()}</b><br>Locations</div><div class="stat"><b>${(s.npcs?.length||0).toLocaleString()}</b><br>NPCs</div><div class="stat"><b>${worldTravel.toLocaleString()}</b><br>World travelers</div><div class="stat"><b>${s.households?.length||0}</b><br>Households</div><div class="stat"><b>${s.factions?.length||0}</b><br>Factions</div></div><div class="cards"><div class="card"><h3>Current Setup</h3><p class="muted">${e(c.settlementSize||'Choose settings')} in ${e(c.province||'province')}. Government: ${e(c.government||'')}. Economy: ${e(c.economy||'')}. Danger: ${e(c.dangerLevel||'')}.</p><div class="box"><strong>Province Time</strong><br>Primary: ${e(tz.primaryUTC||c.timeZone||'UTC')} · Display: ${e((tz.displayUTCs||c.displayUTCs||[]).join(', ')||c.timeZone||'UTC')}<br><span class="muted">Earth-first schedule, Belavadös-second lore date; settlement UTC may refine by longitude.</span></div><div class="tags">${(c.biomeStack||[]).map(b=>`<span class="tag cyan">${e(b.category)} / ${e(b.option)}</span>`).join('')}</div></div><div class="card"><h3>Top Race Results</h3><div class="tags">${chips(topRaces,'violet')||'<span class="muted">Generate NPCs to see race counts.</span>'}</div></div><div class="card"><h3>Alignment Model</h3>${axes.map(a=>`<div class="box"><strong>${e(a.label)}</strong> ${a.min}–${a.max}, neutral ${a.neutral}, step ${a.step}<div class="axisbar"><i style="left:${(a.neutral/a.max)*100}%"></i></div></div>`).join('')}</div><div class="card"><h3>Validation</h3>${warnings.length?warnings.slice(0,14).map(w=>`<div class="notice ${w.level==='warning'?'danger':w.level==='info'?'':'ok'}">${e(w.message)}</div>`).join(''):'<div class="notice ok">No validation warnings yet.</div>'}</div></div>`; };
  LS.renderImports = () => { const rows=(LS.state.imports||[]).map(f=>`<tr><td>${e(f.name)}</td><td>${e(f.status)}</td><td>${Math.round((f.size||0)/1024)} KB</td><td>${Object.entries(f.findings||{}).map(([k,v])=>v?.length?`${e(k)}: ${e(v.slice(0,6).join(', '))}`:'').filter(Boolean).join('<br>')}</td><td>${(f.errors||[]).map(e).join('<br>')}</td></tr>`).join(''); LS.$('imports').innerHTML=`<div id="dropzone" class="dropzone"><h3>Drag and drop source files here</h3><p>Accepts JSON, HTML, TXT, CSV, Markdown, ZIP, DOCX, PDF, and reference images. Text and JSON are scanned directly. Binary files are cataloged safely and fail gracefully in static mode.</p><input id="fileInput" type="file" multiple /></div><div class="divider"></div><div class="table-wrap"><table><tr><th>File</th><th>Status</th><th>Size</th><th>Findings</th><th>Notes</th></tr>${rows||'<tr><td colspan="5">No imports yet.</td></tr>'}</table></div>`; LS.bindImporterEvents?.(); };
  LS.renderLocations = (q='') => { const locs=(LS.state.locations||[]).filter(x=>includesQ(x,q)).slice(0,900); LS.$('locations').innerHTML=`<div class="cards wide">${locs.map(l=>`<article class="card"><h3>${e(l.name)}<small>${e(l.category)} • ${e(l.type)} • ${e(l.district)}</small></h3><div class="tags"><span class="tag cyan">${e(l.biome?.option||'mixed')}</span><span class="tag">${e(l.ownership)}</span><span class="tag ${l.validationNote?'danger':'ok'}">${l.validationNote?'check':'valid'}</span></div><p class="muted">${e(l.description)}</p><div class="box"><strong>People</strong><br>${(l.employees||[]).length} workers · ${(l.residents||[]).length} residents · ${(l.visitors||[]).length} routine visitors</div><div class="box"><strong>Services</strong><ul class="clean">${(l.services||[]).slice(0,5).map(s=>`<li>${e(s.item)} — ${e(s.price)}</li>`).join('')}</ul></div><div class="box"><strong>Hook</strong><br>${e(l.plotHook)}<br><span class="muted">Rumor: ${e(l.rumor)}</span></div></article>`).join('')||'<div class="notice">Generate locations to fill this section.</div>'}</div>${(LS.state.locations||[]).length>900?'<p class="muted">Showing first 900 matching locations. Export JSON for the full directory.</p>':''}`; };
  LS.renderNPCs = (q='') => { const npcs=(LS.state.npcs||[]).filter(x=>includesQ(x,q)).slice(0,650); LS.$('npcs').innerHTML=`<div class="cards wide">${npcs.map(n=>`<article class="card"><h3>${e(n.name)}<small>${e(n.race)}${n.lineage?` / ${e(n.lineage)}`:''} • ${e(n.genderIdentity)} (${e(n.pronouns)}) • ${e(n.lifeStage)}</small></h3><div class="tags">${(n.traits||[]).map(t=>`<span class="tag">${e(t)}</span>`).join('')}<span class="tag cyan">${e(n.profession)}</span>${n.worldTraveler?'<span class="tag violet">world traveler</span>':''}</div><p class="muted">${e(n.biography)}</p><div class="box"><strong>Identity</strong><br>${e(n.publicIdentityNote)}<br>Presentation: ${e(n.socialPresentation)} · Honorific: ${e(n.honorificPreference)} · Terms: ${e(n.relationshipTerminologyPreference)}</div><div class="box"><strong>Home / Work / Transit</strong><br>${e(n.residence)}<br>${e(n.workplaceRole)} ${e(n.profession)} at ${e(n.workplace)}<br><span class="muted">${e(n.publicTransitHabit?.mode||'local travel')} — ${e(n.publicTransitHabit?.fareType||'standard')} (${e(n.publicTransitHabit?.fare||'no fare')})</span></div><div class="box"><strong>Titles</strong><br>${[n.publicTitle,n.factionTitle,n.religiousTitle,n.nobleTitle,n.transitTitle,n.militaryTitle,n.criminalTitle,n.secretTitle].filter(Boolean).map(e).join(' · ')}</div><div class="box"><strong>Alignment</strong>${Object.values(n.alignment?.axes||{}).map(a=>`<div>${e(a.label)} ${a.score} — ${e(a.descriptor)}<div class="axisbar"><i style="left:${(a.score/3000)*100}%"></i></div></div>`).join('')}</div><div class="box"><strong>Secret</strong> ${e(n.secret)}</div></article>`).join('')||'<div class="notice">Generate NPCs to fill this section.</div>'}</div>${(LS.state.npcs||[]).length>650?'<p class="muted">Showing first 650 matching NPCs to protect browser speed. Export JSON for all NPCs.</p>':''}`; };
  LS.renderRelationships = (q='') => { const rels=(LS.state.relationships||[]).filter(x=>includesQ(x,q)).slice(0,900); LS.$('relationships').innerHTML=`<div class="cards wide">${rels.map(r=>`<article class="card"><h3>${e(r.fromName)} → ${e(r.toName)}<small>${e(r.category)} • ${e(r.type)} • ${e(r.relationshipStatus)}</small></h3><p class="muted">${e(r.historyNote||r.history)}</p><div class="tags"><span class="tag">Trust: ${e(r.trustLevel)}</span><span class="tag">Loyalty: ${e(r.loyaltyLevel)}</span><span class="tag danger">Conflict: ${e(r.conflictLevel)}</span><span class="tag cyan">${e(r.publicVisibility)}</span></div><div class="box"><strong>Private Truth</strong> ${e(r.privateTruth||'')}</div><div class="box"><strong>Hook</strong> ${e(r.plotHook||'')}</div></article>`).join('')||'<div class="notice">Generate relationships to fill this section.</div>'}</div>${(LS.state.relationships||[]).length>900?'<p class="muted">Showing first 900 matching relationship records. Export JSON for the full network.</p>':''}`; };
  LS.renderHouseholds = (q='') => { const hhs=(LS.state.households||[]).filter(x=>includesQ(x,q)).slice(0,200); LS.$('households').innerHTML=`<div class="cards wide">${hhs.map(h=>`<article class="card"><h3>${e(h.name)}<small>${e(h.residenceName)} • ${e(h.style)} • ${e((h.familyTypes||[]).join(', '))}</small></h3><p class="muted">${e(h.reputation||h.dynamic)}</p>${LS.familyTreeSVG?.(h)||''}<div class="module-actions"><button class="secondary tiny" data-family-json="${e(h.id)}">Download Tree JSON</button><button class="secondary tiny" data-family-csv="${e(h.id)}">Download Tree CSV</button></div><div class="tags">${(h.members||[]).map(m=>`<span class="tag cyan">${e(m.name)} — ${e(m.role)}</span>`).join('')}</div><div class="box"><strong>Secrets / Conflicts</strong><br>${e((h.secrets||[]).join('; '))}<br><span class="muted">${e((h.conflicts||[]).join('; '))}</span></div><div class="box"><strong>Plot Hooks</strong> ${e((h.plotHooks||[]).join('; '))}</div></article>`).join('')||'<div class="notice">Generate households to fill this section.</div>'}</div>`; LS.$$('[data-family-json]').forEach(b=>b.addEventListener('click',()=>LS.downloadFamilyJSON?.(b.dataset.familyJson))); LS.$$('[data-family-csv]').forEach(b=>b.addEventListener('click',()=>LS.downloadFamilyCSV?.(b.dataset.familyCsv))); };
  LS.renderFactions = (q='') => { const factions=(LS.state.factions||[]).filter(x=>includesQ(x,q)); LS.$('factions').innerHTML=`<div class="cards wide">${factions.map(f=>`<article class="card"><h3>${e(f.name)}<small>${e(f.scope)} • Patron ${e(f.patron)} • Standing ${e(f.standing)}</small></h3><div class="tags"><span class="tag cyan">Leader: ${e(f.leader)}</span><span class="tag danger">Rival: ${e(f.rival)}</span><span class="tag">${e(f.alignmentTest)}</span></div><p class="muted">${e(f.function)}</p><div class="box"><strong>Quest Hook</strong><br>${e(f.questHook)}</div></article>`).join('')||'<div class="notice">Generate factions to fill this section.</div>'}</div>`; };
  LS.renderServices = (q='') => { const services=(LS.state.services||[]).filter(x=>includesQ(x,q)).slice(0,1200); LS.$('services').innerHTML=`<div class="table-wrap"><table><tr><th>Location</th><th>Category</th><th>Item / Service</th><th>Price</th><th>Use</th></tr>${services.map(s=>`<tr><td>${e(s.locationName)}</td><td>${e(s.category)}</td><td>${e(s.item)}</td><td>${e(s.price)}</td><td>${e(s.use)}</td></tr>`).join('')||'<tr><td colspan="5">Generate locations to fill services.</td></tr>'}</table></div>`; };
  LS.renderSchedules = (q='') => { const schedules=(LS.state.schedules||[]).filter(x=>includesQ(x,q)).slice(0,220); LS.$('schedules').innerHTML=`<div class="cards wide">${schedules.map(sc=>`<article class="card"><h3>${e(sc.npcName)}<small>${e(sc.timezone)} • ${e(sc.transit?.mode||'local travel')}</small></h3><div class="box"><strong>Belavadös Date</strong><br>${e(sc.belavadosDate?.month||'')} ${e(sc.belavadosDate?.day||'')}, ${e(sc.belavadosDate?.weekday||'')} ${e(sc.belavadosDate?.clock||'')} · solar ${e(sc.belavadosDate?.belSolarDayProgress||'')}</div><ul class="clean">${(sc.weekly||[]).slice(0,7).map(d=>`<li><strong>${e(d.day)}:</strong> ${e(d.morning)} / ${e(d.midday)} / ${e(d.evening)} / ${e(d.night)}<br><span class="muted">${e(d.route||'')} · delay ${e(d.delayChance||'')} · encounter ${e(d.encounterChance||'')}</span></li>`).join('')}</ul></article>`).join('')||'<div class="notice">Generate schedules to fill this section.</div>'}</div>`; };
  LS.renderEditor = () => { LS.$('editor').innerHTML=`<div class="notice">Edit the generated JSON directly, then apply it. This preserves manual changes and feeds exports.</div><textarea id="jsonEditor" class="json-editor">${e(JSON.stringify(LS.state,null,2))}</textarea><div class="module-actions"><button id="applyJsonEdit" class="gold" type="button">Apply JSON Edits</button><button id="refreshEditor" class="secondary" type="button">Refresh Editor</button></div>`; LS.$('applyJsonEdit')?.addEventListener('click',()=>{ try{ LS.state=JSON.parse(LS.$('jsonEditor').value); LS.state.validations=LS.validateState?.()||[]; LS.saveLocal(); LS.render(); LS.setStatus('JSON edits applied.','ok'); }catch(err){ LS.setStatus('JSON edit failed: '+err.message,'danger'); }}); LS.$('refreshEditor')?.addEventListener('click',()=>LS.render()); };
  LS.renderExport = () => { LS.$('export').innerHTML=`<div class="cards"><div class="card"><h3>JSON Export</h3><p class="muted">Preserves settlement, province, biomes, race cache, NPCs, locations, relationships, family trees, households, schedules, transit, services, factions, intrigue, time, alignment, and imports.</p><button id="exportJsonBtn" class="gold">Download JSON</button></div><div class="card"><h3>HTML Export</h3><p class="muted">Creates a readable campaign page with overview, directories, relationships, family trees, factions, intrigue, schedules, and transit data.</p><button id="exportHtmlBtn" class="gold">Download HTML</button></div><div class="card"><h3>DOCX Export</h3><p class="muted">Creates a minimal real DOCX campaign document with overview, tables-as-paragraphs, hooks, family-tree notes, and DM notes.</p><button id="exportDocxBtn" class="gold">Download DOCX</button></div></div>`; LS.$('exportJsonBtn')?.addEventListener('click',LS.exportState); LS.$('exportHtmlBtn')?.addEventListener('click',LS.exportHTML); LS.$('exportDocxBtn')?.addEventListener('click',LS.exportDOCX); };
  LS.renderOnyx = () => { const pane=LS.$('onyx'); if(!pane) return; pane.innerHTML=`<div class="cards"><div class="card"><h3>Emperor Onyx RuleBot</h3><p class="muted">The local Onyx chat widget was removed from this simulator. Use this button to open the dedicated Emperor Onyx RuleBot page in a new tab.</p><p><a class="button-link gold" href="${e(LS.externalOnyxUrl)}" target="_blank" rel="noopener noreferrer">Open Emperor Onyx RuleBot</a></p><div class="notice">This keeps the Life Simulator focused on settlement/NPC generation while Onyx remains available as his own helper.</div></div></div>`; };
  LS.renderTesting = () => { const counts=LS.data.races.counts||{}; LS.$('testing').innerHTML=`<div class="cards"><div class="card"><h3>Validation Tests</h3><p class="muted">Checks dropdown data, province time zones, generation state, import/export readiness, family-tree tools, transit logic, and no map-pin workflow.</p><button id="runTests" class="gold">Run Browser Smoke Tests</button><div id="testResults" class="box"></div></div><div class="card"><h3>Source Data Counts</h3><ul class="clean"><li>Race categories: ${e(counts.raceCategories)}</li><li>Base race entries: ${e(counts.baseCompendiumRaceEntries)}</li><li>Selectable race/bloodline/subgroup options: ${e(counts.selectableRaceBloodlineSubgroupOptions)}</li><li>Province time zones: ${e((LS.data.time.provinceTimeZones||[]).length)}</li><li>Location pools: ${e(LS.data.content.sourceSummary?.locationPools)}</li><li>Service items: ${e(LS.data.content.sourceSummary?.serviceItems)}</li><li>Province assignments: ${e(LS.data.content.sourceSummary?.provinceAssignments)}</li></ul></div></div>`; LS.$('runTests')?.addEventListener('click',()=>{ const tests=[['22 race categories loaded',(LS.data.races.raceCategories||[]).length===22],['182 selectable race options loaded',counts.selectableRaceBloodlineSubgroupOptions===182],['Province UTC table loaded',(LS.data.time.provinceTimeZones||[]).length>=28],['Settlement targets updated',LS.data.rules.settlementSizeTargets?.['Capital City']?.npcs===3588],['Biome dropdown has five main categories',Object.keys(LS.data.rules.biomeTree||{}).length===5],['No map-pin workflow enabled',LS.data.manifest.mapPinWorkflowIncluded===false],['JSON export function available',typeof LS.exportState==='function'],['HTML export function available',typeof LS.exportHTML==='function'],['DOCX export function available',typeof LS.exportDOCX==='function'],['External Onyx URL configured',!!LS.externalOnyxUrl && !LS.runOnyxCommand],['Family tree CSV function available',typeof LS.familyTreeCSV==='function'],['Generated locations valid',!LS.state.locations?.length||LS.state.locations.every(l=>l.id&&l.name&&l.category)],['Generated NPCs valid',!LS.state.npcs?.length||LS.state.npcs.every(n=>n.id&&n.name&&n.genderIdentity&&n.pronouns&&n.alignment&&n.publicTransitHabit)]]; LS.$('testResults').innerHTML=tests.map(([name,pass])=>`<div class="notice ${pass?'ok':'danger'}">${pass?'PASS':'FAIL'} — ${e(name)}</div>`).join(''); }); };
})();
